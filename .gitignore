# Configuración sensible — nunca versionar
config.php

# Archivos subidos por usuarios
uploads/*
!uploads/.htaccess

# Contadores de rate-limit (estado transitorio, no codigo)
rate_limit/*
!rate_limit/.htaccess

# Artefactos de pruebas locales
.playwright-mcp/
__pycache__/
*.pyc
